<!DOCTYPE html>
<html lang="en">
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>ZeroWaste - Home</title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet">
  <link rel="stylesheet" href="/style.css">

</head>

<body>

  <%- include('partials/navbar') %>
  <main>
  <h1>Available Food Posts</h1>

  <div class="post-container">
    <% if (posts.length > 0) { %>
    <% posts.forEach(post => { %>
      <div class="post-card">
        <img src="<%= post.imageUrl %>" alt="Food Image">
        <p><strong>Posted By:</strong> <%= post.name %></p>

        <div class="description">
          <strong>Food Details:</strong>
            <span>
              <%= post.description.length > 100 ? post.description.substring(0, 100) + '...' : post.description %>
            </span>
        </div>

        <p><strong>Pickup Location:</strong> <%= post.address %></p>
        
        <p>
        <a class="btn" href="https://www.google.com/maps/search/?api=1&query=<%= encodeURIComponent(post.location) %>" target="_blank">
          📍View on Map
        </a>
        
        <% if (post.booked) { %>
          <button class="btn disabled" disabled>❌This Food has been Booked</button>
        <% } else { %>
          <a href="/book/<%= post.id %>" class="btn">Book This Food</a>
        <% } %>
        <p class="text-muted" style="font-size: 10px;">
        Posted on: <%= new Date(post.createdAt).toLocaleString("en-IN") %>
      </p>
      </div>
    <% }); %>

    <% } else { %>
      <p>No posts available. Be the first to donate!</p>
    <% } %>
    </div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</main>
</body>
</html>
