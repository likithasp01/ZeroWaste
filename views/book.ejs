<!DOCTYPE html>
<html>
<head>
  <title>Book Food</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">

</head>

<body>
  <%- include('partials/navbar') %>

  <div class="card">

  <h1>Book This Food</h1>
  <div class="post-container">
    <div class="post-card">

      <img src="<%= post.imageUrl %>" alt="Food Image">
      <p><strong>Name:</strong> <%= post.name %></p>
      <p><strong>Contact:</strong> <%= post.contact %></p>

      <div class="description">
        <strong>Description:</strong>
          <span> <%= post.description.length > 100 ? post.description.substring(0, 100) + '...' : post.description %> </span>
      </div>
      
        <p><strong>Address:</strong> <%= post.address %></p>

        <p class="text-muted" style="font-size: 0.9rem;">
          Posted on: <%= new Date(post.createdAt).toLocaleString("en-IN") %>
        </p>
        <p>
          <a class="btn" href="https://www.google.com/maps/search/<%= encodeURIComponent(post.location) %>" target="_blank">
        📍 Live Location</a>

        </p>
          <% if (!post.booked) { %>
            <form action="/book/<%= index %>" method="POST">
              <button class="btn" type="submit">Confirm Booking</button>
            </form>
          <% } else { %>
            <p class="booked-label">This post is already booked.</p>
          <% } %>

      </div>
    </div>
  </div>

  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
  ></script>

</body>
</html>